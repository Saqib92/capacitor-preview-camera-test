import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cap-camera-preview-test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
